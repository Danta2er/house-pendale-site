# House Pendale Site

Static marketing site for House Pendale / Liora Pendale.

## Local structure

- `index.html` is the landing page.
- `about/`, `books/`, `characters/`, `contact/`, and `join/` hold secondary pages.
- `public/styles/site.css` contains the shared styling.
- `public/scripts/site.js` contains the shared client behavior.
- `public/images/` contains site assets.
- `docs/briefs/` contains the design-side page contract system plus creative and tech handoff briefs.

## Deployment

This folder is linked to the existing Netlify site for `liorapendale.com`.

## Git

- Primary branch: `main`
- Initial import commit: `c49b2b1`
